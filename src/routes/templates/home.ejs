<!DOCTYPE html>
<html lang="en">
    <head>
        <%- include("partials/head") %>
        <title><%= site.name %></title>
    </head>
    <body>
        <div class="container container-y">
            <div><%= site.name %></div>
            <h1>It's the time...</h1>
            <hr />
            <div class="flex-flow">
                <div>
                    <a class="button" href="<%= site.baseUrl %>nyaa">Nyaa</a>
                </div>
            </div>
        </div>
    </body>
</html>
